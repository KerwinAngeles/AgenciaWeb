---

---

<section id="contacto" class="py-20 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-16" data-aos="fade-up">
      <h2 class="text-3xl md:text-4xl font-bold font-heading mb-4">Contáctanos</h2>
      <p class="text-lg text-gray-600 max-w-2xl mx-auto">
        ¿Listo para impulsar tu presencia digital? Cuéntanos sobre tu proyecto y te ayudaremos a alcanzar tus objetivos.
      </p>
    </div>
    
    <div  class="max-w-2xl mx-auto">
      <div data-aos="fade-right">
        <form method="post" class="bg-white rounded-xl shadow-lg p-8">
          <div class="mb-6">
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 mb-2">Nombre</label>
              <input 
                type="text" 
                id="name" 
                class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
                placeholder="Tu nombre"
              />
            </div>
          </div>
          
          <div class="mb-6">
            <label for="subject" class="block text-sm font-medium text-gray-700 mb-2">Asunto</label>
            <input 
              type="text" 
              id="subject" 
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
              placeholder="¿En qué podemos ayudarte?"
            />
          </div>
          
          <div class="mb-6">
            <label for="message" class="block text-sm font-medium text-gray-700 mb-2">Mensaje</label>
            <textarea 
              id="message" 
              rows="5" 
              class="w-full px-4 py-3 rounded-lg border border-gray-300 focus:ring-2 focus:ring-primary focus:border-primary transition-all"
              placeholder="Cuéntanos sobre tu proyecto..."
            ></textarea>
          </div>
          
          <button 
            id="sendButton"
            type="submit" 
            class="w-full bg-primary hover:bg-opacity-90 text-white px-6 py-3 rounded-lg font-medium transition-all"
          >
            Enviar mensaje
          </button>
        </form>
      </div>
    </div>
  </div>
</section>
<script src="/src/scripts/sendMessage.js"></script>
